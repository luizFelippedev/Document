import { useState } from "react"; import { projectService } from "@/services/project.service"; export const useProjects = () => { const [projects, setProjects] = useState([]); const [loading, setLoading] = useState(false); const getProjects = async () => { setLoading(true); try { const data = await projectService.getProjects(); setProjects(data); return data; } catch (error) { console.error(error); } finally { setLoading(false); } }; const getRecentProjects = async (limit = 5) => { try { return await projectService.getRecentProjects(limit); } catch (error) { console.error(error); return []; } }; return { projects, loading, getProjects, getRecentProjects }; }
