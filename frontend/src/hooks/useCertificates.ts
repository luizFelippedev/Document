import { useState } from "react"; import { certificateService } from "@/services/certificate.service"; export const useCertificates = () => { const [certificates, setCertificates] = useState([]); const [loading, setLoading] = useState(false); const getCertificates = async () => { setLoading(true); try { const data = await certificateService.getCertificates(); setCertificates(data); return data; } catch (error) { console.error(error); } finally { setLoading(false); } }; return { certificates, loading, getCertificates }; }
