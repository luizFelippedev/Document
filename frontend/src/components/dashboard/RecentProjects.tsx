import { useState, useEffect } from "react"; import { useProjects } from "@/hooks/useProjects"; import { ProjectCard } from "../projects/ProjectCard"; export const RecentProjects = () => { const { getRecentProjects } = useProjects(); const [projects, setProjects] = useState([]); useEffect(() => { const fetchProjects = async () => { const data = await getRecentProjects(); setProjects(data); }; fetchProjects(); }, []); return <div className="recent-projects"><h2>Recent Projects</h2><div className="projects-grid">{projects.map(project => <ProjectCard key={project.id} project={project} />)}</div></div> }
