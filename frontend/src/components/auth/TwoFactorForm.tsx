import { useState } from "react"; import { useAuth } from "@/hooks/useAuth"; import { Button } from "../ui/Button"; import { Input } from "../ui/Input"; export const TwoFactorForm = () => { const [code, setCode] = useState(""); const { verifyTwoFactorCode } = useAuth(); const handleSubmit = (e) => { e.preventDefault(); verifyTwoFactorCode(code); }; return <form onSubmit={handleSubmit}><Input value={code} onChange={(e) => setCode(e.target.value)} placeholder="Verification Code" /><Button type="submit">Verify</Button></form> }
