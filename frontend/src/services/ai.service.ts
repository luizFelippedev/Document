import { apiGet, apiPost } from "@/utils/api"; const BASE_URL = "/api/ai"; export const aiService = { generateDescription: async (data) => { return apiPost(`${BASE_URL}/generate-description`, data); }, analyzeProject: async (projectId) => { return apiGet(`${BASE_URL}/analyze-project/${projectId}`); }, getRecommendations: async (userId) => { return apiGet(`${BASE_URL}/recommendations/${userId}`); } }
