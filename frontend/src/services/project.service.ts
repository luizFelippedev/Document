import { apiGet, apiPost, apiPut, apiDelete } from "@/utils/api"; const BASE_URL = "/api/projects"; export const projectService = { getProjects: async () => { return apiGet(BASE_URL); }, getProject: async (id) => { return apiGet(`${BASE_URL}/${id}`); }, createProject: async (projectData) => { return apiPost(BASE_URL, projectData); }, updateProject: async (id, projectData) => { return apiPut(`${BASE_URL}/${id}`, projectData); }, deleteProject: async (id) => { return apiDelete(`${BASE_URL}/${id}`); }, getRecentProjects: async (limit = 5) => { return apiGet(`${BASE_URL}/recent?limit=${limit}`); } }
