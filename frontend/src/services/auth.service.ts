import { apiGet, apiPost, apiPut, apiDelete } from "@/utils/api"; const BASE_URL = "/api"; export const authService = { login: async (email, password) => { return apiPost(`${BASE_URL}/auth/login`, { email, password }); }, register: async (userData) => { return apiPost(`${BASE_URL}/auth/register`, userData); }, logout: async () => { return apiPost(`${BASE_URL}/auth/logout`, {}); }, getCurrentUser: async () => { return apiGet(`${BASE_URL}/auth/me`); }, forgotPassword: async (email) => { return apiPost(`${BASE_URL}/auth/forgot-password`, { email }); }, resetPassword: async (token, password) => { return apiPost(`${BASE_URL}/auth/reset-password`, { token, password }); }, verifyEmail: async (token) => { return apiGet(`${BASE_URL}/auth/verify-email/${token}`); } }
