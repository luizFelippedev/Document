import { apiGet, apiPost, apiPut, apiDelete } from "@/utils/api"; const BASE_URL = "/api/certificates"; export const certificateService = { getCertificates: async () => { return apiGet(BASE_URL); }, getCertificate: async (id) => { return apiGet(`${BASE_URL}/${id}`); }, createCertificate: async (certificateData) => { return apiPost(BASE_URL, certificateData); }, updateCertificate: async (id, certificateData) => { return apiPut(`${BASE_URL}/${id}`, certificateData); }, deleteCertificate: async (id) => { return apiDelete(`${BASE_URL}/${id}`); }, verifyCertificate: async (code) => { return apiGet(`${BASE_URL}/verify/${code}`); } }
